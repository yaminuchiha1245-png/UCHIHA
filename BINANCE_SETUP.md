# إعداد Binance Pay ID في UCHIHA

أضف المتغيرات التالية داخل **Railway → UCHIHA → Variables**:

```env
BINANCE_AUTO_PAY_ENABLED=1
BINANCE_COIN=USDT
BINANCE_VERIFICATION_MODE=reference
BINANCE_VERIFICATION_PROVIDER=binance_pay
BINANCE_PAY_ID=ضع_رقم_Pay_ID_العام_هنا
BINANCE_API_KEY=ضع_مفتاح_API_للقراءة_فقط
BINANCE_API_SECRET=ضع_السر_هنا
BINANCE_MIN_AMOUNT=1
BINANCE_MAX_AMOUNT=1000
BINANCE_POLL_SECONDS=60
BINANCE_PAYMENT_WINDOW_MINUTES=120
```

يدعم النظام أيضًا اسم التفعيل القديم:

```env
BINANCE_PAYMENT_ENABLED=1
```

## الحصول على Pay ID

1. افتح تطبيق Binance ثم **Pay**.
2. افتح ملفك في Binance Pay وانسخ **Pay ID** الرقمي العام.
3. ضع الرقم فقط في `BINANCE_PAY_ID` من دون مسافات أو رابط.
4. أنشئ API Key بصلاحية القراءة فقط، ولا تفعّل السحب إطلاقًا.

إذا منعت Binance الاتصال من خادم Railway، تبقى طريقة الدفع ظاهرة ويستطيع العميل
الإرسال إلى Pay ID، لكن البوت لا يضيف الرصيد تلقائيًا. يحجز رقم المعاملة ويرسل الطلب
إلى مراجعة الإدارة، وهذا يمنع الشحن بناءً على رقم يكتبه العميل فقط.

## طريقة اعتماد الدفعة

يعرض البوت المبلغ نفسه الذي اختاره العميل دون زيادة. بعد نجاح الدفع يرسل العميل
`Transaction ID` من تفاصيل Binance Pay، ثم يقرأ البوت سجل معاملات Pay ويتحقق من:

- أن المعاملة واردة إلى حساب Binance المرتبط بالمفتاح.
- أن المبلغ موجب ومطابق تمامًا.
- أن العملة هي USDT وأن نوع العملية Pay أو C2C.
- وقوع العملية ضمن مهلة الطلب.
- عدم استخدام المعرف في أي طلب سابق.

لا يُضاف الرصيد من رقم المعاملة وحده؛ يجب أن يظهر الرقم نفسه في السجل الرسمي.

## الاختبار بعد النشر

1. افتح البوت بحساب الأدمن.
2. افتح **لوحة الإدارة → مركز Binance**.
3. اضغط **فحص الربط**.
4. يجب أن تظهر رسالة نجاح وPay ID وعدد معاملات Pay خلال 24 ساعة.
5. اضغط **مزامنة الآن** للتأكد من أن الفحص اليدوي يعمل.
6. نفّذ اختبارًا بمبلغ صغير قبل اعتماد النظام تجاريًا.

لا تضع مفاتيح Binance داخل GitHub أو هذا الملف.

## وضع TRON المستقبلي

الكود ما زال يحتفظ بوضع `trongrid` وعنوان USDT-TRC20 للمستقبل، لكنه لا يظهر للعميل
عندما تكون قيمة `BINANCE_VERIFICATION_PROVIDER=binance_pay`.
