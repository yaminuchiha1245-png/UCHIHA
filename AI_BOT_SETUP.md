# UCHIHA AI Bot Product

هذا المنتج هو قالب بوت ذكاء اصطناعي جاهز للبيع داخل منظومة UCHIHA. كل نسخة مباعة تستخدم Telegram Bot Token خاص بالعميل، بينما مفتاح OpenAI يبقى على الخادم ولا يظهر للمستخدم أو للأدمن داخل تيليجرام.

## ما تم تنفيذه

- واجهة رئيسية فيها زر PRO في الأعلى.
- نماذج تجارية قابلة للتعديل من قاعدة البيانات.
- `UCHIHA AI V1` مجاني افتراضيًا ومربوط بنموذج اقتصادي.
- `UCHIHA AI V2` افتراضيًا PRO ومقفل لغير المشترك.
- صفحة قدرات لكل نموذج: الذكاء، التحليل، الصور، البرمجة، والتعليم.
- أوضاع: محادثة عامة، برمجة، تعليم ودراسة، وإنشاء صور.
- Responses API للنصوص وImages API للصور.
- حفظ سياق محادثة محدود لكل مستخدم.
- لوحة إدارة داخل تيليجرام عبر `/admin`.
- إدارة النماذج، تفعيل/إيقاف النموذج، وتحويله مجاني/PRO.
- إدارة المستخدمين، منح PRO، إلغاؤه، الحظر وفك الحظر.
- إحصائيات الاستهلاك وTokens محليًا.
- قسم OpenAI مع زر يفتح صفحة الفوترة الرسمية لتجديد الرصيد.
- مفاتيح OpenAI وTelegram تُقرأ من متغيرات البيئة فقط.

## التشغيل

```bash
pip install -r requirements.txt
python ai_bot_launcher.py
```

استخدم `.env.ai.example` كمرجع للأسماء المطلوبة، لكن ضع القيم الحقيقية في Railway Variables.

## أوامر الأدمن الحالية

```text
/admin
/aiaddmodel الاسم | openai-model | pro
/airename ID | الاسم الجديد
/aiprovider ID | openai-model
/aipro USER_ID DAYS
/aiban USER_ID
/aiunban USER_ID
```

## ملاحظة الألوان في Telegram

Telegram Bot API يدعم حاليًا أنماط أزرار محددة (primary أزرق، success أخضر، danger أحمر، أو النمط الافتراضي الشفاف). لذلك زر V1 يستخدم `primary` ليظهر أزرق، ونموذج PRO المقفول يبقى شفافًا مع رمز القفل. زر PRO العلوي يستخدم رمزًا أصفر `🟡` لأن Telegram لا يوفر لون زر أصفر مستقلًا.

## الخطوة التالية داخل UCHIHA Builder

بعد تثبيت قالب البوت نفسه، يتم ربطه بعملية شراء المنتج داخل المنصة بحيث تنشئ المنصة نسخة/instance للعميل، تحفظ إعداداته وهويته وTelegram token بشكل آمن، وتربطه بحساب OpenAI المركزي دون كشف المفتاح.
