# FABLE5 Website Completion Report

## الحالة النهائية

- تم تطوير الموقع على الفرع `fable5/website-hardening`.
- نجح GitHub Actions على Python 3.12 في التشغيل رقم `6`.
- تم دمج Pull Request رقم `18` إلى فرع `main` بتاريخ 24 يوليو 2026.
- Merge commit: `a3d5ce5521091ac3ea2089e1be20b119d31727f9`.
- اكتمل نشر Railway بنجاح بعد الدمج.
- لم تُضف أسرار أو ملفات `.env` أو قواعد بيانات إلى GitHub.

## ما تم إنجازه

### الشراء ومنتجات JS4Card

- تفعيل واجهة الكميات الثابتة والخيارات المتعددة والمدى والحد الأدنى والأقصى والخطوة في الصفحة الفعلية.
- إظهار الحقول المطلوبة وقيم المزود الخام بصورة صحيحة.
- الحفاظ على دقة أسعار JS4Card الصغيرة في الكتالوج وحساب الإجمالي.
- حفظ مفتاح إعادة محاولة الشراء لمدة 30 دقيقة لمنع إنشاء طلب مكرر بعد انقطاع الشبكة.

### الحماية

- حد افتراضي لجسم الطلب قدره 5 MiB.
- Rate limiting لتسجيل الدخول والشراء والشحن وربط البوت والمزامنة وعمليات الإدارة.
- فرض `no-store` على بيانات الحساب والمحفظة والطلبات والإدارة.
- إضافة رؤوس أمان ومعرّف `X-Request-ID` ورسائل خطأ عامة آمنة.

### لوحة الإدارة

- إدارة ظهور المنتجات وإخفائها وترتيبها وتعيين اسم مخصص داخل الموقع.
- حفظ تعديلات المنتجات في جدول مستقل حتى لا تلغيها مزامنة JS4Card.
- رفع شعار المتجر وأيقونة التطبيق من الهاتف وإعادتهما للوضع الافتراضي.
- إدارة العملة الأساسية وعلامتها وأسعار التحويل بصيغة JSON.
- إدارة سياسة الخصوصية والشروط والأحكام وسياسة الاسترجاع.
- إظهار روابط السياسات أسفل المتجر وإنشاء صفحات عامة لها.
- استخدام الشعار والأيقونة الجديدة في واجهة المتجر ولوحة الإدارة وملف PWA الديناميكي.

## البيانات والسلامة

الجداول الجديدة إضافية فقط:

- `storefront_product_overrides`
- `storefront_branding`

لم يتم حذف أو استبدال جداول المنتجات أو الطلبات أو الأرصدة أو عمليات الدفع. لم تتغير عقود البوت أو JS4Card أو Binance Pay أو TRC20 أو Sham Cash.

## الاختبارات الناجحة

```bash
python -m compileall -q .
python -m unittest tests/test_storefront_core.py
python -m unittest tests/test_storefront_hardening.py
python -m unittest tests/test_storefront_management.py
python -m unittest tests/test_binance_compat.py
python tests/binance_integration_smoke.py
```

كما نجح بناء ورفع حزمة `UCHIHA-FABLE5-DEVELOPED.zip` النظيفة دون قواعد بيانات أو أسرار أو اختبارات.

## ملاحظات تشغيلية

- لم يتم تنفيذ عملية دفع حقيقية أو طلب JS4Card حي أثناء الاختبارات الآلية؛ الاختبارات المالية كانت Offline smoke tests فقط.
- Rate limiting الحالي محفوظ داخل عملية التشغيل؛ عند تشغيل عدة replicas يفضّل لاحقًا استخدام Redis أو مخزن مشترك.
