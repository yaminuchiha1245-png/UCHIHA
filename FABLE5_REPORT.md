# FABLE5 Website Hardening Report

## النطاق

تم العمل على الفرع `fable5/website-hardening` فقط، انطلاقًا من `main` عند commit:

`a77e1c561111a743d9f3377fe88862bce21c7c75`

لم يتم دمج الفرع في `main`، ولم يتم النشر إلى Railway، ولم يتم تعديل قاعدة البيانات أو حذف أي بيانات.

## نتائج المراجعة

1. نظام الشراء الديناميكي موجود أصلًا ولا يحتاج إعادة بناء:
   - كميات ثابتة وخيارات متعددة ومدى حر مع حد أدنى وأقصى وخطوة.
   - حقول JS4Card الإلزامية والاختيارات التي تحفظ القيمة الخام المطلوبة من المزود.
   - تحقق على الخادم وعدم الاعتماد على الواجهة فقط.
   - حجز ذري للرصيد ومنع التكرار باستخدام `purchase_token`.
   - دقة أسعار JS4Card الصغيرة قبل حساب الإجمالي.

2. كانت تعديلات HTML الخاصة بالكميات والخيارات ودقة السعر تُطبّق على `storefront_api._STOREFRONT_HTML`، بينما مسار الموقع يستورد `storefront_theme.STOREFRONT_HTML` وقت الطلب؛ لذلك كان من الممكن عرض النسخة الأصلية بدل المعدلة.

3. قائمة المنتجات العامة كانت تقرّب السعر إلى منزلتين قبل وصوله للواجهة، ما قد يعرض منتجات JS4Card ذات سعر الوحدة الصغير كـ `0.00` أو بسعر غير دقيق.

4. واجهة الشراء كانت تنشئ `Idempotency-Key` جديدًا لكل محاولة. انقطاع الشبكة بعد تثبيت الطلب وقبل وصول الرد كان قد يؤدي إلى إعادة المحاولة بمفتاح مختلف.

5. لم تكن هناك طبقة موحّدة تحد حجم أجسام طلبات API الحساسة، أو تهدئ محاولات تسجيل الدخول والشراء والشحن والمزامنة المتكررة، أو تفرض `no-store` على كل البيانات الخاصة.

## الإصلاحات المنفذة

### إظهار حقول وكميات JS4Card في الصفحة الفعلية

- نشر نسخة HTML المعدلة إلى `storefront_theme.STOREFRONT_HTML` بعد تركيب:
  - `js4card_purchase_options`
  - `js4card_price_precision`
- أصبحت الصفحة التي يعيدها الموقع تستخدم حقول المنتج وخيارات الكمية والخطوات والأسعار الدقيقة فعليًا.

### دقة السعر في الكتالوج

- استعادة سعر JS4Card الدقيق من قاعدة البيانات بعد استعلام الكتالوج العام.
- إضافة `precise_price` للمنتجات التي تحتاج عرضًا دقيقًا.
- استخدام العرض الدقيق عند الحاجة وإبقاء عرض العملات العادي لبقية المنتجات.

### منع تكرار الشراء بعد انقطاع الشبكة

- حفظ نية الشراء ومفتاح idempotency في `sessionStorage` لمدة 30 دقيقة حسب المنتج والكمية والخيار والحقول.
- إعادة استخدام المفتاح نفسه بعد إعادة المحاولة أو تحديث الصفحة.
- حذف المفتاح فقط بعد وصول استجابة ناجحة من الخادم.
- هذا يكمل الحماية الذرية الموجودة أصلًا في الخادم ولا يستبدلها.

### حماية الطلبات الحساسة

أضيفت طبقة ASGI مستقلة لا تغيّر البوت أو تدفقات الدفع، وتشمل:

- حد افتراضي لجسم الطلب 5 MiB، مع فحص `Content-Length` والطلبات المرسلة على أجزاء.
- rate limiting محدود الذاكرة لدخول العميل والإدارة، الشراء، الشحن، ربط تيليجرام، مزامنة JS4Card، وعمليات الإدارة الكتابية.
- استجابة عربية `429` مع `Retry-After`.
- `X-Request-ID` لكل طلب لتتبع الأخطاء دون كشف أسرار.
- رسالة عامة آمنة للأخطاء غير المتوقعة بدل كشف stack trace للعميل.
- فرض `Cache-Control: no-store` على الحساب والمحفظة والطلبات والإدارة.
- رؤوس أمان موحدة: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-Opener-Policy`, `Cross-Origin-Resource-Policy`, و`Strict-Transport-Security` عند HTTPS.

## الملفات الأساسية

- `storefront_hardening.py` — الحماية ودقة الكتالوج ونية الشراء الثابتة.
- `storefront_launcher.py` — تركيب الطبقة بعد وحدات JS4Card الحالية.
- `tests/test_storefront_hardening.py` — اختبارات الضوابط الجديدة.
- `.env.example` — الإعدادات الاختيارية الجديدة.
- `.github/workflows/fable5-validate-package.yml` — التحقق الكامل وبناء ZIP نظيف.
- `FABLE5_REPORT.md` — هذا التقرير.

## الاختبارات والتحقق

تم تشغيل GitHub Actions على **Python 3.12**، ونجحت الخطوات التالية:

```bash
python -m compileall -q .
python -m unittest tests/test_storefront_core.py
python -m unittest tests/test_storefront_hardening.py
python -m unittest tests/test_binance_compat.py
python tests/binance_integration_smoke.py
```

النتائج:

- compile لكل ملفات Python: ناجح.
- اختبارات storefront core: ناجحة.
- اختبارات الحماية الجديدة: **8 اختبارات ناجحة**.
- اختبارات توافق Binance: ناجحة.
- Binance offline integration smoke test: ناجح.
- بناء ورفع حزمة التشغيل: ناجح.

اختبارات الحماية تغطي rate limiting، رفض الطلبات الكبيرة، رؤوس الأمان و`no-store`، الحفاظ على cache العام، استعادة السعر الدقيق، وثبات مفتاح إعادة محاولة الشراء.

## حزمة التشغيل

تم إنشاء `UCHIHA-FABLE5-DEVELOPED.zip` من الملفات المتتبعة اللازمة للتشغيل، مع استبعاد:

- `.env` و`.env.example`.
- قواعد البيانات وملفات SQLite.
- الاختبارات وGitHub Workflows.
- logs وcache وbackup وZIP المتداخلة.
- التقارير وملفات الشرح.

نتيجة الفحص:

- عدد الملفات: **25**.
- الحجم التقريبي: **581 KB**.
- `unzip -t`: ناجح بلا أخطاء.
- الملفات الأساسية `bot.py`, `uchiha.py`, `storefront_launcher.py`, `storefront_hardening.py`, `storefront_api.py`, `storefront_core.py`, و`requirements.txt`: موجودة.
- لا توجد ملفات قاعدة بيانات أو `.env` أو اختبارات داخل الحزمة.

## متغيرات البيئة الجديدة

كلها اختيارية؛ القيم الافتراضية تعمل دون إضافتها إلى Railway:

```env
STOREFRONT_MAX_BODY_BYTES=5242880
STOREFRONT_RATE_LIMIT_KEYS=10000
STOREFRONT_TRUST_PROXY=0
```

- `STOREFRONT_MAX_BODY_BYTES`: الحد الأقصى لجسم الطلب، من 64 KiB إلى 12 MiB.
- `STOREFRONT_RATE_LIMIT_KEYS`: الحد الأعلى لمفاتيح limiter المحفوظة في الذاكرة.
- `STOREFRONT_TRUST_PROXY`: فعّله فقط خلف Proxy موثوق خارج Railway؛ Railway يُكتشف تلقائيًا.

لا توجد أسرار أو Tokens جديدة.

## ما لم يتم تنفيذه عمدًا

- لم يتم الدمج في `main`.
- لم يتم النشر إلى Railway.
- لم يتم إجراء طلب JS4Card حي أو دفع حقيقي باستخدام Binance Pay أو TRC20 أو Sham Cash.
- لم يتم تعديل أو حذف أي جدول أو طلب أو سجل مالي.

## المخاطر والعمل المتبقي

1. rate limiting الحالي process-local؛ عند تشغيل عدة replicas يفضّل Redis أو مخزن مشترك.
2. يلزم staging smoke test لمنتجات تمثل الكمية الثابتة، خيارات الكمية، المدى والخطوة، الحقول الإلزامية، وقيم المزود الخام.
3. الحد الافتراضي 5 MiB يسمح بصورة إثبات أصلية قرابة 3 MiB بعد base64، متوافقًا مع الحد الحالي.
4. لم تتغير عقود JS4Card أو Binance أو TronGrid أو Sham Cash، لكن يلزم اختبار staging قبل الدمج بسبب مشاركة عملية التشغيل نفسها.
5. لوحة الإدارة ما زالت تحتاج دفعة لاحقة لإدارة الشعار والأيقونة والسياسات وترتيب المنتجات مباشرة.

## قرار الدمج

الفرع مناسب للمراجعة كـ Draft PR، لكنه **غير مدموج وغير منشور**. يوصى بإجراء staging smoke test ثم الدمج يدويًا فقط بعد التأكد من تدفقات الشراء والدفع الحقيقية.
