# FABLE5 Website Hardening Report

## النطاق

تم العمل على الفرع `fable5/website-hardening` فقط، انطلاقًا من `main` عند commit:

`a77e1c561111a743d9f3377fe88862bce21c7c75`

لم يتم دمج الفرع في `main`، ولم يتم النشر إلى Railway، ولم يتم تعديل قاعدة البيانات أو حذف أي بيانات.

## ما تم اكتشافه

1. نظام الشراء الديناميكي موجود أصلًا ولا يحتاج إعادة بناء:
   - كميات ثابتة وخيارات كمية متعددة ومدى حر مع حد أدنى وأقصى وخطوة.
   - حقول JS4Card الإلزامية والاختيارات التي تحفظ القيمة الخام المطلوبة من المزود.
   - تحقق على الخادم وعدم الاعتماد على الواجهة فقط.
   - حجز ذري للرصيد ومنع التكرار باستخدام `purchase_token`.
   - دقة أسعار JS4Card الصغيرة قبل حساب الإجمالي.

2. كانت تعديلات HTML الخاصة بالكميات والخيارات ودقة السعر تُطبّق على `storefront_api._STOREFRONT_HTML`، بينما مسار الموقع يستورد `storefront_theme.STOREFRONT_HTML` وقت الطلب. هذا كان يسمح بعرض النسخة الأصلية بدل النسخة المعدلة في بعض مسارات التشغيل.

3. قائمة المنتجات العامة كانت تعيد تقريب السعر إلى منزلتين قبل وصوله للواجهة، ما قد يعرض منتجات JS4Card ذات سعر الوحدة الصغير كـ `0.00` أو بسعر غير دقيق.

4. واجهة الشراء كانت تنشئ `Idempotency-Key` جديدًا لكل محاولة. عند انقطاع الشبكة بعد تثبيت الطلب وقبل وصول الرد، قد يعيد العميل المحاولة بمفتاح مختلف.

5. لم تكن هناك طبقة موحّدة تحد حجم أجسام طلبات API الحساسة أو تهدئ محاولات تسجيل الدخول والشراء والشحن والمزامنة المتكررة.

6. بعض استجابات الحساب الحساسة كانت تعتمد على كل endpoint لضبط الكاش، بدل فرض `no-store` مركزيًا.

## ما تم إصلاحه

### تفعيل تعديلات الشراء في الصفحة الفعلية

- نشر نسخة HTML المعدلة إلى `storefront_theme.STOREFRONT_HTML` بعد تركيب وحدات:
  - `js4card_purchase_options`
  - `js4card_price_precision`
- النتيجة: حقول المنتج، خيارات الكمية، والخطوات والأسعار الدقيقة تظهر في الصفحة التي يعيدها الموقع فعليًا.

### دقة السعر في الكتالوج

- استرجاع سعر JS4Card الدقيق من قاعدة البيانات بعد استعلام الكتالوج العام.
- إضافة علامة `precise_price` للمنتجات التي تحتاج عرضًا دقيقًا.
- استخدام عرض ثلاثي المنازل عند الحاجة، مع إبقاء عرض العملات العادي لبقية المنتجات.

### منع التكرار بعد انقطاع الشبكة

- حفظ نية الشراء ومفتاح idempotency في `sessionStorage` لمدة 30 دقيقة حسب:
  - المنتج
  - الكمية
  - الخيار
  - الحقول المدخلة
- يعاد استخدام المفتاح نفسه عند إعادة المحاولة أو تحديث الصفحة.
- يحذف المفتاح فقط بعد وصول استجابة ناجحة من الخادم.
- الحماية الجديدة تكمل المنع الذري الموجود أصلًا في الخادم ولا تستبدله.

### حماية الطلبات الحساسة

أضيفت طبقة ASGI مستقلة لا تغيّر البوت أو تدفقات الدفع، وتشمل:

- حد افتراضي لجسم الطلب: 5 MiB، مع فحص `Content-Length` والطلبات المرسلة على أجزاء.
- rate limiting محدود الذاكرة للمسارات الحساسة:
  - دخول وإنشاء حساب العميل.
  - دخول الإدارة.
  - الشراء.
  - إنشاء/تحقق الشحن.
  - ربط تيليجرام.
  - مزامنة JS4Card.
  - عمليات الإدارة الكتابية.
- رسالة عربية واضحة مع `429` و`Retry-After`.
- معرّف `X-Request-ID` لكل طلب لتسهيل تتبع الأخطاء دون كشف أسرار.
- رسالة عامة آمنة للأخطاء غير المتوقعة بدل كشف stack trace للعميل.
- فرض `Cache-Control: no-store` على بيانات الحساب والمحفظة والطلبات والإدارة.
- رؤوس أمان موحدة، منها:
  - `X-Content-Type-Options`
  - `X-Frame-Options`
  - `Referrer-Policy`
  - `Permissions-Policy`
  - `Cross-Origin-Opener-Policy`
  - `Cross-Origin-Resource-Policy`
  - `Strict-Transport-Security` عند HTTPS

## الملفات الأساسية المعدلة

- `storefront_hardening.py` — طبقة الحماية، دقة الكتالوج، ونية الشراء الثابتة.
- `storefront_launcher.py` — تركيب الطبقة الجديدة بعد وحدات JS4Card الحالية.
- `tests/test_storefront_hardening.py` — اختبارات مركزة للضوابط الجديدة.
- `.env.example` — توثيق الإعدادات الاختيارية الجديدة.
- `.github/workflows/fable5-validate-package.yml` — تحقق كامل وبناء ZIP نظيف.
- `FABLE5_REPORT.md` — هذا التقرير.

## الاختبارات والتحقق

تم تشغيل GitHub Actions على **Python 3.12**، ونجحت جميع الخطوات التالية:

```bash
python -m compileall -q .
python -m unittest tests/test_storefront_core.py
python -m unittest tests/test_storefront_hardening.py
python -m unittest tests/test_binance_compat.py
python tests/binance_integration_smoke.py
```

النتائج:

- compile لكل ملفات Python: ناجح.
- اختبارات storefront core: ناجحة.
- اختبارات الحماية الجديدة: **8 اختبارات ناجحة**.
- اختبارات توافق Binance: ناجحة.
- Binance offline integration smoke test: ناجح.
- بناء ورفع حزمة التشغيل: ناجح.

اختبارات الحماية تغطي:

1. sliding-window rate limiter.
2. تصنيف المسارات الحساسة.
3. رفض جسم الطلب الكبير قبل وصوله للتطبيق.
4. حظر محاولة الشراء الزائدة بـ `429`.
5. فرض `no-store` ورؤوس الأمان على API الخاص.
6. الحفاظ على cache العام للكتالوج.
7. استعادة سعر JS4Card الدقيق من قاعدة البيانات.
8. تثبيت مفتاح نية الشراء وإعادة استخدامه.

## حزمة التشغيل

تم إنشاء `UCHIHA-FABLE5-DEVELOPED.zip` من الملفات المتتبعة اللازمة للتشغيل، مع استبعاد:

- `.env` و`.env.example`.
- قواعد البيانات وملفات SQLite.
- الاختبارات وGitHub Workflows.
- ملفات logs وcache وbackup وZIP المتداخلة.
- التقارير وملفات الشرح.

نتيجة فحص الحزمة:

- عدد الملفات: **25**.
- الحجم: **581112 بايت**.
- SHA-256: `01e16e3d575ae3715b9efae684813ed9d17bf588ec75d1c40e9e57f815ab7506`
- فحص سلامة ZIP: ناجح، بلا أخطاء.
- الملفات الأساسية `bot.py`, `uchiha.py`, `storefront_launcher.py`, `storefront_hardening.py`, `storefront_api.py`, `storefront_core.py`, و`requirements.txt`: موجودة.

## متغيرات البيئة الجديدة

كلها اختيارية؛ القيم الافتراضية تعمل دون إضافتها إلى Railway:

```env
STOREFRONT_MAX_BODY_BYTES=5242880
STOREFRONT_RATE_LIMIT_KEYS=10000
STOREFRONT_TRUST_PROXY=0
```

- `STOREFRONT_MAX_BODY_BYTES`: الحد الأقصى لجسم الطلب، من 64 KiB إلى 12 MiB.
- `STOREFRONT_RATE_LIMIT_KEYS`: الحد الأعلى لمفاتيح limiter المحفوظة في الذاكرة.
- `STOREFRONT_TRUST_PROXY`: فعّله فقط خلف Proxy موثوق خارج Railway؛ Railway يُكتشف تلقائيًا.

لا توجد أسرار أو Tokens جديدة.

## ما لم يتم تنفيذه عمدًا

- لم يتم الدمج في `main`.
- لم يتم النشر إلى Railway.
- لم يتم إجراء طلب JS4Card حي أو دفع حقيقي باستخدام Binance Pay أو TRC20 أو Sham Cash.
- لم يتم تعديل أو حذف أي جدول أو طلب أو سجل مالي.

## المخاطر والعمل المتبقي

1. rate limiting الحالي process-local. إذا أصبح الموقع يعمل على عدة replicas، يفضّل نقل العدادات إلى Redis أو مخزن مشترك.
2. يجب اختبار تدفق الشراء كاملًا على staging باستخدام منتجات تمثل:
   - كمية ثابتة.
   - خيارات كمية.
   - مدى حر وخطوة.
   - حقل إلزامي.
   - خيار مزود بقيمة raw مختلفة عن الاسم المعروض.
3. يجب مراقبة حجم صور إثبات الدفع. الحد الافتراضي 5 MiB يسمح لصورة base64 أصلية حتى قرابة 3 MiB، متوافقًا مع الحد الحالي في التطبيق.
4. لم يتم تغيير عقود JS4Card أو Binance أو TronGrid أو Sham Cash، لكن يلزم smoke test على staging قبل الدمج بسبب مشاركة عملية التشغيل نفسها.
5. لوحة الإدارة الحالية ما زالت تحتاج دفعات لاحقة لإدارة الشعار والأيقونة والسياسات وترتيب المنتجات مباشرة من الواجهة.

## قرار الدمج

الفرع مناسب للمراجعة كـ Draft PR، لكنه **غير مدموج وغير منشور**. يوصى بعد مراجعة الكود بإجراء staging smoke test ثم الدمج يدويًا فقط بعد التأكد من تدفقات الشراء والدفع الحقيقية.
