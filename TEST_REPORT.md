# تقرير اختبار نسخة Binance

تم تشغيل الاختبارات التالية بنجاح:

- `python uchiha.py --check-code`
- `python -m py_compile` لجميع ملفات Python المعدلة.
- `python -m unittest discover -s tests -v` — نجح **19 اختبارًا**.
- تشمل المجموعة `test_binance_compat.py` و`test_tron_verifier.py` و`test_storefront_core.py` و`test_shamcash_admin.py`.
- `python tests/binance_integration_smoke.py`
- فحص Ruff للأخطاء الحرجة `E9,F63,F7,F82`.
- فحص JavaScript لواجهة العميل ولوحة الإدارة عبر `node --check`.
- اختبار DOM للواجهتين: الدخول أولًا، فتح التطبيق بعد المصادقة، تحميل السلايدر والأقسام والمنتجات، وفتح نافذة المنتج الديناميكية.

## ما يتحقق منه اختبار Uchiha Store

- إنشاء الحساب وتشفير كلمة المرور وتسجيل الدخول والجلسة الآمنة.
- منع تكرار البريد أو اسم المستخدم.
- إنشاء رابط Telegram آمن لمرة واحدة.
- نقل الرصيد والطلبات من حساب الموقع إلى حساب Telegram عند الربط دون فقدان.
- منع إعادة استخدام رمز الربط.
- الإعدادات والألوان الافتراضية وصور الهوية الأصلية.
- التحقق من حقول طلب JS4Card قبل التنفيذ.

## ما يتحقق منه اختبار التكامل

- تهيئة قاعدة البيانات.
- ظهور زر **مركز Binance** في لوحة الأدمن.
- اختبار اتصال TronGrid عبر عميل مزيف دون إرسال أسرار أو طلبات مالية.
- قبول عنوان TRON الصحيح ورفض العنوان أو TXID غير الصالح.
- قراءة حدث `Transfer` المثبت من عقد USDT الرسمي فقط.
- رفض العقد المزيف، عنوان المستلم الخطأ، والمعاملة الفاشلة أو غير المثبتة.
- صحة توقيع HMAC وإرسال `X-MBX-APIKEY` والمسارات الرسمية لعنوان الإيداع والسجل.
- إنشاء طريقة الدفع التلقائية.
- إنشاء بطاقة دفع العميل بالمبلغ نفسه دون أي زيادة أو كسر تعريفي.
- طلب `TXID / Hash` من العميل والتحقق منه آليًا.
- عدم اعتماد طلب وضع المعرف اعتمادًا على المبلغ وحده.
- مطابقة إيداع USDT مؤكد مع المعرف والطلب الصحيحين.
- رفض المبلغ أو الشبكة أو الحالة غير المطابقة.
- إضافة الرصيد مرة واحدة فقط.
- منع تكرار اعتماد نفس TXID وإضافة الرصيد مرة ثانية.
- ظهور الطريقة داخل واجهة المتجر، إنشاء الفاتورة، إرسال TXID، ثم تحديث رصيد حساب الموقع.
- إيقاف الفحص اليدوي عندما يوقف الأدمن Binance من اللوحة.

## الاختبار الحي المطلوب بعد Railway

الاختبار المحلي لا يحتوي مفاتيحك الحقيقية. بعد وضع متغيرات Railway اضغط:

**لوحة الإدارة → مركز Binance → فحص الربط**

عند ظهور **فحص Binance ناجح** مع رقم آخر كتلة تكون قراءة شبكة TRON الحية قد نجحت. بعد ذلك يلزم اختبار تحويل حقيقي صغير للتأكد من العنوان وإعدادات Railway معًا.

## ما يتحقق منه اختبار Sham Cash

- ظهور مركز Sham Cash داخل لوحة إدارة البوت حتى قبل وجود التوكن.
- رفض روابط API غير المشفرة أو التي تحتوي بيانات دخول داخل الرابط.
- قبول API Token تجريبي وهمي وقراءة الحسابات والأرصدة عبر عميل مزيف.
- عدم ظهور التوكن في رسائل النجاح أو الخطأ.
- عرض طرق Sham Cash وطلباتها مع بقاء الاعتماد التلقائي مغلقًا إلى أن يصل توثيق الحركات الرسمي.
