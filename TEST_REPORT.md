# تقرير اختبار نسخة Binance

تم تشغيل الاختبارات التالية بنجاح:

- `python uchiha.py --check-code`
- `python -m py_compile` لجميع ملفات Python المعدلة.
- `python -m unittest tests/test_binance_compat.py`
- `python -m unittest tests/test_storefront_core.py`
- `python tests/binance_integration_smoke.py`
- فحص Ruff للأخطاء الحرجة `E9,F63,F7,F82`.
- فحص JavaScript لواجهة العميل ولوحة الإدارة عبر `node --check`.
- اختبار DOM للواجهتين: الدخول أولًا، فتح التطبيق بعد المصادقة، تحميل السلايدر والأقسام والمنتجات، وفتح نافذة المنتج الديناميكية.

## ما يتحقق منه اختبار Uchiha Store

- إنشاء الحساب وتشفير كلمة المرور وتسجيل الدخول والجلسة الآمنة.
- منع تكرار البريد أو اسم المستخدم.
- إنشاء رابط Telegram آمن لمرة واحدة.
- نقل الرصيد والطلبات من حساب الموقع إلى حساب Telegram عند الربط دون فقدان.
- منع إعادة استخدام رمز الربط.
- الإعدادات والألوان الافتراضية وصور الهوية الأصلية.
- التحقق من حقول طلب JS4Card قبل التنفيذ.

## ما يتحقق منه اختبار التكامل

- تهيئة قاعدة البيانات.
- ظهور زر **مركز Binance** في لوحة الأدمن.
- اختبار الاتصال عبر عميل مزيف دون إرسال أسرار أو طلبات مالية.
- إنشاء طريقة الدفع التلقائية.
- مطابقة إيداع USDT مؤكد مع الطلب الصحيح.
- إضافة الرصيد مرة واحدة فقط.
- منع تكرار اعتماد نفس TXID وإضافة الرصيد مرة ثانية.
- إيقاف الفحص اليدوي عندما يوقف الأدمن Binance من اللوحة.

## الاختبار الحي المطلوب بعد Railway

الاختبار المحلي لا يحتوي مفاتيحك الحقيقية. بعد وضع متغيرات Railway اضغط:

**لوحة الإدارة → مركز Binance → اختبار الاتصال**

عند ظهور **اختبار Binance ناجح** يكون الاتصال الحي والتوقيع وصلاحية قراءة سجل الإيداعات قد نجحت.
